define([
    'jquery',
    'underscore',
    'backbone'
], function($, _, Backbone) {

    var router = {
        initialize: function(res) {
            return "Router Initialize Called" + res;
        }
    }
    return router;
});
